name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: 
- workflow_dispatch
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          path: '' # Empty path means checkout everything
          fetch-depth: 0 # Fetch all history for your workflows to have access
      - run: |
          ls -a