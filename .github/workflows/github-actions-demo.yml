name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: 
- workflow_dispatch
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - run: |
          echo SECRET ${{ secrets.USERNAME }}
          echo VARS ${{ vars.FOO }}